# CLAUDE.md

Project instructions for this repository live in **[AGENTS.md](AGENTS.md)**. Read it
before making any change here.

It covers the build commands, the repository structure and page data flow, the
critical gotchas (`_data/*.yml` is the source of truth, ordering is decided by
templates, UTF-8 without BOM), and step-by-step guides for the recurring tasks.

Nothing from it is repeated here. Keeping exactly one copy is what stops these
documents from drifting apart.
